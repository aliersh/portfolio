---
import { technologies } from "@/data/technologies";
import TechIcon from "./TechIcon";

const stackTechnologies = [
    technologies.typescript,
    technologies.react,
    technologies.tailwind,
    technologies.nextjs,
    technologies.astro,
];
---

<section class="mb-8" aria-labelledby="tech-stack-heading">
    <h2 
        id="tech-stack-heading" 
        class="text-md font-bold font-khula text-gray-800 mb-4"
    >
        Tech Stack
    </h2>
    <div 
        class="flex gap-4 items-center justify-start"
        role="list"
        aria-label="Technologies I work with"
    >
        {stackTechnologies.map((tech) => (
            <div role="listitem">
                <TechIcon 
                    Icon={tech.Icon} 
                    name={tech.name} 
                    href={tech.href} 
                />
            </div>
        ))}
    </div>
</section> 